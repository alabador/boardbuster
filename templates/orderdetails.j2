<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>BoardBuster - Manage Orders</title>
    <link rel="stylesheet" href="../static/style.css">
</head>
<body>
    <header>
        <nav>
            <a href="/">Home</a> |
            <a href="/boardgames">Manage Board Games</a> |
            <a href="/customers">Manage Customers</a> |
            <a href="/orders">Manage Orders</a> |
            <a href="/orderdetails">Order Details</a> |
            <a href="/rentals">Manage Rentals</a> |
            <a href="/rentaldetails">Rental Details</a> |
            <a href="/categories">Manage Categories</a>
        </nav>
    </header>

    <h1>Order Details</h1>
    <form id="add" action="/orderdetails" method="POST">
        <h2>Add New Order Item</h2>
        <label for="addOrderID">Order ID:</label>                
        <select name="order" id="addOrderID" required>
            {% if orders %}
            {% for key in orders %}
            <option value="{{key['orderID']}}">{{key['orderID']}}</option>
            {% endfor %}
            {% endif %}
        </select>
        <label for="addGameID">Game ID:</label>                
        <select name="game" id="addgameID" required>
            {% if games %}
            {% for key in games %}
            <option value="{{key['gameID']}}">{{key['gameID']}}</option>
            {% endfor %}
            {% endif %}
        </select>
        <label>Price</label> <input type="number" step="0.01" name="price" required>
        <label>Quantity:</label> <input type="number" step="1" min="0" name="quantity" required>
        <input type="submit" value="Add Order Detail" name="Add_Order_Detail" class="btn">
    </form>

    <h2>Existing Orders</h2>
    <table>
        {% if orderdetails %}
        <tr>
            <!-- Iterate through each key in the first entry to get the
            column name -->

            {% for key in orderdetails[0].keys() %}

            <!-- Create a <th> tag with the key inside of it, this will be
            our header row -->
            <th>{{ key }}</th>

            <!-- End of this for loop -->
            {% endfor %}

            <!-- Extra header row for edit and delete buttons -->
            <th></th>
        </tr>
        {% for detail in orderdetails %}
        <tr>
            {% for key in detail.keys() %}
            <td>{{detail[key]}}</td>
            {% endfor %}
            <td>
                <a href="edit_orderdetail/{{detail.orderDetailID}}" class="btn">Edit</a>
                <a href="delete_orderdetail/{{detail.orderDetailID}}" class="btn">Delete</a>
            </td>
        </tr>
        {% endfor %}
        {% endif %}
    </table>

    <footer>
        <p>© 2024 Austin Labador & Vincent Le</p>
    </footer>
    <!-- 
    Citation for the following function:
    Date: 11/7/2024
    Adapted from CS 340 Exploration - Web Application Technology
    Used the concept of hiding and showing forms, as well as the source code as a basis. 
    Adapted this to fit the needs of this form.
    Source URL: https://canvas.oregonstate.edu/courses/1976520/pages/exploration-web-application-technology?module_item_id=24719064
    -->
    <script language="JavaScript">

        function closeForm() {
            document.getElementById('edit').style.display = 'none';
            document.getElementById('add').style.display = 'block';
        }
    </script>
</body>
</html>
